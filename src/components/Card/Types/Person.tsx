import { Heading, Text } from "@chakra-ui/react";
import { CardItem } from "../CardItem";

export default function Person({

}: {

  }) {
  return (
    <CardItem rowSpan={2} colSpan={1}>
      个人信息
    </CardItem>
  );
}